export interface FileMetadata {
  /**
   * File format, if applicable.
   * @type {string}
   * @example "text/csv"
   */
  contentType: string

  /**
   * File content length.
   * @type {[type]}
   */
  contentLength?: string
}
